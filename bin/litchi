#!/usr/bin/env ruby

require 'litchi-fm'
require 'thor'

class LitchiFmCLI < Thor
  desc "download <yaml>", "Download the mp3 according to the yaml file"
  option :yaml
  def download(yaml)
    LitchiFm::Downloader.new(yaml).start
  end
end

LitchiFmCLI.start(ARGV)
